<h1> <%=@quiz.name%> </h1>

<% @quiz.questions.each do |q|%>
  <%= q[:content] %>
  <ul>
  <% q.answers.each do |a| %>
    <li><%= a.content %></li>
  <% end %>
  </ul>
<% end %>

<INPUT TYPE="button" VALUE="Back" onClick="history.go(-1);">
