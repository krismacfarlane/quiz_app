<h1> Welcome <%=@instructor.name%></h1>

<ul>
   <% @instructor.cohorts.each do |c|  %>
    <li>
      <%= button_to c.name.capitalize, cohort_path, method: :get, :class => :button %>
    </li>
   <%end%>
 </ul>

 <%= button_to 'Logout', logout_path, method: :delete %>
