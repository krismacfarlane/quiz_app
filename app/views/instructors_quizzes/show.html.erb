<h1> <%=@quiz.name%> </h1>

<% @quiz.questions.each do |q|%>
  <%= q[:content] %>
  <ul>
  <% q.answers.each do |a| %>
    <li><%= a.content %></li>
  <% end %>
  </ul>
<% end %>

<%= button_to 'Back to all quizzes ' , instructors_quizzes_path, method: :get, :class => :button %>
